import 'package:flutter/material.dart';

class Showhabit extends StatelessWidget {
  const Showhabit({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('ddddd'),
    );
  }
}
